;;; Directory Local Variables         -*- no-byte-compile: t; -*-
;;; For more information see (info "(emacs) Directory Variables")

((nil
  .
  ((eglot-workspace-configuration
    .
    ;; Find options here: https://rust-analyzer.github.io/book/configuration.html
    ( :rust-analyzer
      ;; See (info "(eglot) JSONRPC objects in Elisp") for formatting options.
      ;; Example for setting rust-analyzer.linkedProjects:
      ;; ( :linkedProjects ["Cargo.toml"])
      )))))
